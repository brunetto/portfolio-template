{% for gallery in galleries %}
    <section id="{{ gallery.name }}" class="section">
        <h2 class="section-title">
            {{ gallery.title }}
        </h2>
        <div class="gallery">
            {% for photo in gallery.photos %}
                {% image photo,
                gallery.title %}
            {% endfor %}
        </div>
    </section>
{% endfor %}